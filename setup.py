import setuptools

# The license is declared here (rather than in pyproject.toml) so the metadata stays
# compatible with both setuptools<77 and the newer SPDX license expression handling.
setuptools.setup(license="AGPL-3.0-or-later")
