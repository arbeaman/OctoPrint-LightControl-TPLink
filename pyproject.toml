[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "OctoPrint-LightControl-TPLink"
version = "2.0.1"
description = "Adds support for TPLink smart plugs and power strips that speak the original **Kasa** protocol to OctoPrint-LightControl as a sub-plugin. **Tapo** devices, and plugs that only expose TP-Link's newer encrypted **KLAP** protocol are not supported."
authors = [
    {name = "Alerick Beaman", email = "35195829+arbeaman@users.noreply.github.com"}
]
readme = {file = "README.md", content-type = "text/markdown"}
dynamic = ["license"]
requires-python = ">=3.7, <4"

# any additional requirements besides OctoPrint should be listed here
dependencies = []

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = [
    "octoprint_lightcontrol_tplink",
    "octoprint_lightcontrol_tplink.*"
]

[project.entry-points."octoprint.plugin"]
lightcontrol_tplink = "octoprint_lightcontrol_tplink"

[project.urls]
Homepage = "https://github.com/arbeaman/OctoPrint-LightControl-TPLink"
